name: Docs

on:
  # Allow to manually trigger through github API
  workflow_dispatch:

  # Allow the release pipeline to gate publishing on the docs building
  workflow_call:

  # Triggers with push to a pr aimed at master
  pull_request:
    branches:
      - master

# Superseded runs on a pull request get cancelled. Runs started through
# workflow_call share the calling pipeline's group, so they are left alone.
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:

  build:
    name: Sphinx html + doctest
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v7

    - name: Setup Python
      uses: actions/setup-python@v7
      with:
        python-version: "3.12"

    - name: Install uv
      uses: astral-sh/setup-uv@v9.0.0

    - name: Install grakel
      run: |
        uv sync --locked

    - name: Build docs and run doctests
      run: |
        cd doc
        uv run --with "sphinx>=6,<9" --with "sphinx-gallery>=0.15" \
          --with "sphinx-rtd-theme" --with "sphinxcontrib-bibtex>=2.5" \
          --with "numpydoc" --with "matplotlib" --with "pillow" \
          make clean html doctest

    # Handed to the release pipeline, which publishes it to github pages
    - name: Upload built docs
      uses: actions/upload-artifact@v7
      with:
        name: docs-html
        path: doc/_build/html
